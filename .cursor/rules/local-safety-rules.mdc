---
# 规则名称：用于在本仓库内启用本地安全协作约束。
description: Local safety rules for NebulaGrid repository work
# 作用范围：alwaysApply=true 表示此规则应始终作为本地工作提醒。
alwaysApply: true
---

# NebulaGrid 本地安全规则

<!--
本文件是本地规则文件，用于记录在 NebulaGrid 仓库内工作的安全约束。
创建本文件只会新增 `.cursor/rules/local-safety-rules.mdc`，不会移动、覆盖或删除仓库中的其他文件。
-->

## 1. 不要误删文件

<!--
在执行任何可能改变文件树的操作前，先确认命令只影响目标路径。
禁止在没有明确确认目标的情况下运行批量删除命令，例如 `rm -rf *`、`find ... -delete` 或清理整个目录的脚本。
-->

- 修改前先查看 `git status --short --branch`，确认当前工作区状态。
- 修改后再次查看 `git status --short --branch`，确认变更范围符合预期。
- 若发现非预期删除、重命名或大范围改动，应立即停止并先恢复或询问下一步。

## 2. 保持变更最小化

<!--
每次任务只改动完成需求所必需的文件，避免顺手格式化、重排或重写无关文件。
这样可以降低误删、误改以及合并冲突的风险。
-->

- 只创建或编辑与当前任务直接相关的文件。
- 不修改生成物、依赖目录或缓存目录，除非任务明确要求。
- 不引入与当前需求无关的配置、依赖或脚本。

## 3. 注释与说明要求

<!--
当新增规则、脚本或配置时，应写清楚每一段规则的目的。
注释应解释“为什么需要这样做”，而不只是重复代码或配置本身。
-->

- 新增配置时，为关键字段添加说明性注释。
- 新增脚本或代码时，为主要步骤、风险点和边界条件添加详细注释。
- 面向协作者的规则应使用清晰、可执行、可检查的语言。

## 4. 提交前检查

<!--
提交前检查用于确认本地规则文件确实存在，并确认没有意外删除。
如果检查结果显示删除标记（例如 `D path/to/file`），不要提交，先排查原因。
-->

- 使用 `git status --short --branch` 检查变更清单。
- 使用 `git diff -- .cursor/rules/local-safety-rules.mdc` 复核本规则文件内容。
- 确认变更符合用户需求后再提交。
