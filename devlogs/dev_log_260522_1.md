# NebulaGrid 开发日志更新

**文件名**：`dev_log_260522_1.md`  
**日期**：2026-05-22  
**项目**：NebulaGrid 3.0  
**记录类型**：代码阅读与开发进度更新（更正版）  
**分析对象**：重新上传的 `NebulaGrid-0522.zip`  
**评估口径**：按照当前实际开发顺序进行阶段性评估  
**开发顺序**：节点管理 → 用户管理 → 文件管理 → 环境管理 → 任务管理

---

## 1. 本次更新结论

本次重新检查以新上传的 `NebulaGrid-0522.zip` 为准。和上一份 22 日分析相比，这个压缩包的重点确实不同：**本轮最主要的推进不是环境管理继续新增功能，而是节点管理和页面优化明显完善**。

当前仓库中可以看到最近提交包括：

- `complete env manage`
- `complete node management`
- `Page Optimization`

并且仓库中包含 `test_result.md`，其中记录了 2026-05-22 对管理员节点管理功能的实际接口测试结果。测试内容覆盖节点新增、查询、修改、重连、强制下线、删除、所有人选择、私有/共享可见性和前端静态文件检查。

因此，本次更正后的阶段判断为：

> **节点管理已经从“基础可用”推进到“管理面基本完成”；用户管理和文件管理保持较高完成度；环境管理仍处于中后段；任务管理仍未正式进入主开发阶段。**

按照当前开发顺序，整体工程完成度可估计为 **73%–75%**。

其中，节点管理因为本轮新增了完整的管理员 CRUD、多所有人、共享范围、GPU 型号顺序、删除清理引用和前端搜索选择等能力，阶段完成度可上调到 **82%–85%**。

---

## 2. 本次主要新增或变化内容

| 方向 | 上一阶段状态 | 当前状态 | 当前影响 |
|---|---|---|---|
| 管理员节点新增 | 已有基础新增 | 新增字段更完整，支持所有人、访问范围、共享范围、带宽、GPU 型号顺序 | 节点登记更接近真实管理需求 |
| 管理员节点修改 | 不完整或未重点确认 | 已实现 `PUT /api/admin/nodes/{node_id}` | 管理员可编辑节点基础信息、所有人和 GPU 清单 |
| 管理员节点删除 | 不完整或未重点确认 | 已实现 `DELETE /api/admin/nodes/{node_id}`，并清理调度引用 | 节点退役流程初步形成 |
| 多所有人 | 旧字段偏单 owner | 新增 `owner_user_ids` JSON 字段，并兼容旧 `owner_user_id` | 一个节点可以归属多个用户 |
| 节点访问范围 | 主要依赖 `is_public` | 新增 `access_scope`，支持 public/private | 节点可见性表达更清晰 |
| 节点共享范围 | 无明确模型 | 新增 `sharing_scope`，支持 none/group/public | 私有节点可进一步控制共享范围 |
| 所有人校验 | 基础校验 | 后端校验所有 owner 必须来自用户表 | 避免保存悬空用户 ID |
| GPU 型号顺序 | 基础登记 | `gpu_count` 必须与 `gpu_models` 数量对应，按 index 保持顺序 | 后续调度能按 GPU index 正确分配 |
| 节点可见性 | 管理员视角为主 | 普通用户按所有人、public、group 共享关系过滤可见节点 | 为后续任务选择节点打基础 |
| 组内共享 | 无或较弱 | 根据导师-学生关系展开共享可见性 | 导师/学生组内节点共享开始成型 |
| 删除节点清理 | 不完整 | 删除节点时清理 `TaskRequirement.node_id`、`EnvInstallJob.target_node_id`、`TaskAllocation`、`TaskRuntimeGuard` | 避免节点退役后外键引用阻塞 |
| 节点操作审计 | 基础 | create/update/delete/reconnect/force_offline 均记录审计 | 节点管理操作可追踪 |
| 前端节点管理 | 基础表单 | 新增节点修改、删除、所有人搜索、多选、使用权/共享范围展示 | 管理员节点页更接近可用 |
| 前端页面优化 | 局部样式 | 左侧导航固定、所有人搜索布局优化 | 使用体验提升 |
| 实际测试记录 | 无专门结果 | `test_result.md` 记录管理员节点管理接口测试通过 | 节点管理完成度有测试依据 |
| 已知问题 | 未明确 | `gpu_count` 与 `gpu_models` 不一致时后端拒绝，但 HTTP 返回 500 | 需要改为 400/422 以便前端友好提示 |

---

## 3. 当前开发进度总览

| 模块 | 主要代码位置 | 当前状态 | 持久化情况 | 前端对接 | 进度 | 当前问题 / 下一步 |
|---|---|---|---|---|---:|---|
| 需求与架构文档 | `docs/*.md`, `README.md`, `devlogs/*.md` | 文档较完整，并已保存多轮开发日志 | 文档型 | 手册页面可读取 | 92% | 后续需继续随实现同步修订 |
| 后端 FastAPI 框架 | `backend/app/main.py`, `api/router.py` | API 入口、异常处理、CORS、路由聚合已完成 | 不涉及 | 已对接 | 76% | 启动流程已有初始化逻辑，后续需增强部署自检 |
| 时间与响应工具 | `core/time_utils.py`, `core/responses.py` | 已有本地时间工具，统一响应 request_id 使用时间前缀 | 不涉及 | 间接对接 | 70% | 后续建议明确 UTC 存储、本地时间展示策略 |
| 配置系统 | `core/config.py` | 环境变量配置较完整，系统设置已落库 | 环境变量 + DB Setting | 间接对接 | 68% | 仍缺完整配置文件与热加载体系 |
| 统一响应与错误处理 | `core/responses.py`, `core/errors.py` | 统一响应格式可用 | 不涉及 | 已对接 | 76% | 节点字段校验错误目前仍可能返回 500，需要统一转 400/422 |
| RBAC 权限 | `core/rbac.py` | 角色权限表已实现 | 代码常量 | 已对接 | 62% | 后续可扩展项目组、资源池、队列权限 |
| 数据库模型 | `db/models/core.py` | 用户、会话、节点、GPU、文件任务、环境、环境操作日志、包、安装作业、任务、审计、设置等表已定义 | PostgreSQL / SQLAlchemy | 间接对接 | 82% | 节点字段、文件任务、审计、设置、环境本体已接入；任务和异步环境安装仍未完全接入 |
| 数据库初始化 | `db/init_db.py` | 建表、默认 admin、默认 settings、补列逻辑可用；本轮包含节点新字段补列 | PostgreSQL | 间接对接 | 72% | 仍建议引入 Alembic，替代手写补列 |
| 认证登录 | `api/auth.py`, `auth_service.py` | 登录、退出、当前用户、改名、改密码已实现 | `users`, `login_sessions` | 已对接 | 78% | 已具备基础可用性 |
| 登录会话管理 | `auth_service.py`, `api/admin.py` | 当前用户设备、管理员在线用户、指定用户上线记录、手动下线已实现 | `login_sessions` | 已对接 | 76% | 可继续优化在线判断与实时推送 |
| 账号启用/停用 | `user_service.py`, `auth_service.py` | 停用后不可登录，且已有会话会被吊销 | `users.state`, `login_sessions` | 已对接 | 85% | 已满足主要需求 |
| 用户管理 | `api/users.py`, `user_service.py` | 创建、查询、编辑、重置密码、删除已实现 | `users` | 已对接 | 85% | 剩余生产级细节主要在 Linux 账户回滚和跨模块权限 |
| 最后管理员保护 | `user_service.py`, `tests/test_api.py` | 防止删除、停用、降级最后管理员 | 数据库判断 | 已对接 | 80% | 已具备关键保护 |
| 导师-学生关系 | `UserSupervisor`, `user_service.py` | 学生最多两名导师；导师只能管理自己学生 | `user_supervisors` | 已对接 | 78% | 已开始被节点 group 共享和导师学生文件视图复用 |
| Linux 子账户映射 | `linux_account_service.py` | 目录创建、权限命令、可选 sudo、密码同步、删除账户流程较完整 | 部分写入 `users` 字段 | 前端展示 | 68% | 真实 sudoers 部署、失败回滚和异常修复策略仍需加强 |
| 管理员节点登记 | `api/admin.py`, `node_service.py`, `schemas/nodes.py` | 新增、修改、删除、重连、强制下线均已实现 | `nodes`, `gpus` | 已对接 | 85% | 已知问题：GPU 数量/型号不一致时应返回 400/422，而不是 500 |
| 节点所有人与共享 | `Node.owner_user_ids`, `access_scope`, `sharing_scope`, `can_user_access_node` | 多所有人、private/public、none/group/public 共享已实现 | `nodes.owner_user_ids`, `nodes.access_scope`, `nodes.sharing_scope` | 已对接 | 82% | 后续可继续扩展节点组、队列、资源池 |
| 节点列表与资源展示 | `api/nodes.py`, `node_service.py` | 可列出计算节点，过滤 master/control-plane，并按用户权限过滤可见节点 | `nodes`, `gpus`, `task_allocations` | 已对接 | 78% | GPU 占用展示依赖后续调度器写入 allocation |
| 节点监控 worker | `workers/node_monitor.py`, `remote/monitor.py` | 可 SSH 采集 CPU/RAM/网络/GPU，并同步 GPU 清单 | PostgreSQL + InfluxDB | 节点页读取最新快照 | 58% | 依赖真实部署、远端脚本同步和 systemd 管理 |
| InfluxDB 指标 | `metrics_service.py` | 支持写入和读取最新快照 | InfluxDB | 间接对接 | 52% | 仍缺历史曲线、异常指标处理和前端趋势图 |
| 仪表盘 | `dashboard_service.py` | 节点统计走 DB，用户可见 GPU 按节点可见性计算；任务统计仍依赖任务服务 | 混合 | 已对接 | 55% | 等任务管理阶段再统一改为 DB 查询 |
| 文件浏览 | `file_service.py`, `path_resolver.py` | 目录列表、文本/二进制/图片/音视频预览真实可用 | 文件系统 | 已对接 | 82% | 已接近可用工作台 |
| 文件写操作 | `api/files.py`, `file_service.py` | 上传、下载、mkdir、新建、保存、复制、移动、重命名、删除均已真实落盘 | 文件系统 | 已对接 | 82% | 仍需上传大小限制、分片上传、配额、危险操作二次确认 |
| 导师查看学生文件 | `file_service.py`, frontend file scope | 导师可只读浏览名下学生文件，非名下学生不可见 | 文件系统 + `user_supervisors` | 已对接 | 78% | 当前是只读视图，符合安全边界；后续可考虑授权下载/批注能力 |
| 文件打包/解压 | `file_service.py`, `frontend/src/app.js` | zip/tar 打包/解压后台任务 + 进度展示 | 文件系统 + `file_jobs` | 已对接 | 78% | 需加强压缩炸弹检测 |
| 文件路径安全 | `path_resolver.py`, `file_service.py` | 虚拟路径映射用户 home，拒绝危险根操作，解压检查路径逃逸与软链接 | 文件系统 | 已对接 | 78% | 建议继续补 symlink、超大压缩包、并发删除测试 |
| 文件任务状态 | `FileJob` | 打包/解压任务状态已落库，重启后会处理中断任务 | PostgreSQL `file_jobs` | 已对接 | 75% | 后续可考虑独立 worker 或 Redis/队列化 |
| 审计日志 | `audit_service.py`, `AuditLog` 模型 | 已从内存改为数据库记录，节点/环境/文件等动作已分类 | PostgreSQL `audit_logs` | 已对接 | 72% | 后续可补导出、保留策略和环境操作日志联动查询 |
| 系统设置 | `audit_service.py`, `Setting` 模型 | 已从内存改为数据库读写 | PostgreSQL `settings` | 已对接 | 70% | 后续可增强类型校验、分组和 worker 热加载 |
| 使用手册 | `manual_service.py`, frontend manual | 按角色返回文档 | 文件系统 | 已对接 | 65% | 仍可继续拆分真正分角色手册 |
| 前端主界面 | `frontend/src/app.js`, `styles.css` | 单页应用功能较完整，文件、环境、节点管理界面增强明显 | localStorage + API | 已对接 | 84% | 左侧导航固定、节点所有人搜索等体验已优化 |
| 管理员后台前端 | `frontend/src/app.js` | 总览、节点、用户、登录管理、设置、审计页面已实现 | API | 已对接 | 84% | 节点管理页已具备增删改查和所有人搜索 |
| 导师学生管理前端 | `frontend/src/app.js` | 支持导师创建、编辑、重置学生密码 | API | 已对接 | 76% | 后续需继续贯通到环境和任务管理 |
| 环境登记与列表 | `api/envs.py`, `env_service.py` | 已从内存环境转向 DB Env 表，并支持扫描同步 | PostgreSQL `envs` | 已对接 | 68% | `list_envs` 可见性逻辑仍需确认 registered 环境是否会被隐藏 |
| 环境导入 | `env_service.py`, frontend env page | 支持从用户文件区选择 zip 包后台导入 | PostgreSQL + 文件系统 + 日志 | 已对接 | 65% | 目前主要支持 zip；仍需压缩炸弹限制和独立 worker 化 |
| 环境复制 | `clone_env`, `run_env_clone` | 支持复制已有环境并修复路径 | PostgreSQL + 文件系统 + 日志 | 已对接 | 65% | 大环境复制耗时长，仍在 API 进程后台线程中运行 |
| 环境检测 | `inspect_env_runtime`, `remote/env_probe.py` | 可通过 conda activate 后检测 Python/框架/包列表 | 实时返回 + 日志落盘 | 已对接 | 70% | 依赖 conda activate、探针脚本同步和真实运行环境 |
| 环境操作日志 | `EnvOperationLog`, `env_operation_log_path` | 同时写文件日志和数据库记录 | PostgreSQL + 文件系统 | 已对接读取文件日志 | 75% | DB 日志还缺独立分页查询页面 |
| 本机环境包安装 | `install_local_package`, `run_env_install_command` | 支持 conda 离线包、whl、requirements 批量、源码目录安装 | 文件系统 + 环境日志 + EnvOperationLog | 已对接 | 68% | 当前同步执行，长安装会占用 API 请求；失败回滚和 manifest 未完成 |
| 已安装包删除 | `preview_delete_installed_packages`, `delete_installed_packages` | 支持删除前预览、核心包保护、conda/pip 分源卸载 | 环境日志 + EnvOperationLog | 已对接 | 68% | 删除依赖关系不会自动修复，需要用户确认 |
| EnvPackage/EnvInstallJob 队列链路 | `upload_package`, `install_package`, `env_install_worker.py` | 表已定义，但服务层仍主要用 `_PACKAGES/_JOBS` | 服务层内存，worker 扫 DB，脱节 | 旧接口仍存在 | 25% | 下一步要把包上传和安装 job 真正写入数据库 |
| compile 模式 | `EnvPackageInstallRequest.mode=compile` | 请求模型已有，但执行链路未实现 | 未完成 | 前端显示等待任务调度接入 | 10% | 需等节点/GPU 编译执行策略明确后实现 |
| 任务 API | `api/tasks.py`, `task_service.py` | 提交、查询、取消、重提、日志占位、guard 占位已实现 | **内存 `_TASKS`** | 已对接 | 30% | 符合当前开发顺序，等任务阶段集中处理 |
| 任务数据库模型 | `Task`, `TaskRequirement`, `TaskAllocation`, `TaskEvent` | 表定义较完整 | PostgreSQL | 未真正接入任务服务 | 45% | 后续任务阶段重写 task_service |
| 调度器 | `workers/scheduler.py` | 只扫描 DB 中 `state=wait` 的任务并打印数量 | PostgreSQL | 未对接 | 10% | 等任务阶段实现 GPU 分配 |
| 执行器 | `workers/task_executor.py`, `remote/runner.py` | runner 可启动命令、写 PID；executor 只扫描 dispatching | runner 写文件，executor 未写状态 | 未真正对接 | 20% | 等任务阶段实现 SSH 启动和状态回写 |
| Runtime Guard | `workers/runtime_guard.py`, `TaskRuntimeGuard` | 只扫描 guard 记录并打印数量 | 表已定义但未真正写入 | 任务详情 guard 占位 | 10% | 等任务阶段实现 PID/GPU 检查 |
| 任务日志 | `task_service.py` | 日志接口返回占位文本 | 无 | 已对接抽屉 | 15% | 等任务阶段读取真实 stdout/stderr |
| 部署文件 | `deploy/systemd/.gitkeep`, docs | 文档有部署说明，文件未完全落地 | 文档型 | 无 | 35% | 需要补 systemd、Nginx、远端脚本同步工具 |
| 后端工具脚本 | `backend/scripts/*.py` | 初始化 DB、重置 admin、删除旧 metrics 表 | PostgreSQL | 无 | 52% | 仍缺一键部署、迁移、远端分发 |
| 测试 | `backend/tests/test_api.py`, `test_result.md` | 单元/冒烟测试覆盖基础功能；`test_result.md` 记录节点管理接口实测通过 | 不适用 | 不适用 | 55% | 需要把 `test_result.md` 中的节点用例沉淀为自动化测试 |
| 旧版 DDLTM 2.0 | `ddltm2.0/` | 保留旧系统参考代码 | JSON/SQLite/内存 | 独立旧系统 | 100%（旧系统参考） | 继续作为迁移参考，不建议直接扩展 |

---

## 4. 按开发顺序的阶段性评估

## 4.1 节点管理

**当前进度：约 82%–85%**

这是本轮更正后最需要上调的模块。当前管理员节点管理已经覆盖新增、修改、删除、重连、强制下线、节点所有人、共享范围、GPU 清单顺序和权限可见性。

| 功能点 | 当前状态 | 说明 |
|---|---|---|
| 节点登记 | 已实现 | 管理员可新增计算节点 |
| 节点修改 | 已实现 | 支持修改名称、IP、SSH 用户、所有人、共享范围、GPU 型号 |
| 节点删除 | 已实现 | 删除节点并清理相关调度引用 |
| 节点列表 | 已实现 | 管理员和用户均可查询节点 |
| master/control-plane 过滤 | 已实现 | 避免主控节点出现在计算节点列表 |
| GPU 清单登记 | 已实现 | 可手填 GPU 型号并按 index 保存 |
| GPU 数量/型号校验 | 已实现 | 数量不一致会拒绝请求，但错误码需优化 |
| 多所有人 | 已实现 | `owner_user_ids` 支持多个用户 |
| 访问范围 | 已实现 | `access_scope` 支持 public/private |
| 共享范围 | 已实现 | `sharing_scope` 支持 none/group/public |
| 所有人搜索 | 已实现 | 前端可搜索统一识别码、用户名、姓名 |
| 节点可见性过滤 | 已实现 | 普通用户只能看到有权限的节点 |
| 组内共享 | 已实现 | 基于导师-学生关系展开共享用户 |
| 节点重连 | 已实现 | 状态置为 reconnecting |
| 强制下线 | 已实现 | 状态置为 manual_offline，并关闭调度 |
| 删除清理引用 | 已实现 | 清理 TaskRequirement、EnvInstallJob、TaskAllocation、TaskRuntimeGuard 引用 |
| 节点操作审计 | 已实现 | create/update/delete/reconnect/force_offline 均记录 |
| 实际接口测试 | 已有记录 | `test_result.md` 记录主要链路测试通过 |

### 当前剩余问题

| 问题 | 建议 |
|---|---|
| `gpu_count` 与 `gpu_models` 不一致时返回 HTTP 500 | 应改为统一 400/422，并给前端友好错误文案 |
| 节点监控部署仍依赖真实环境 | 补 systemd、远端脚本同步、SSH 探活失败重试 |
| 节点标签/队列/资源池尚未建立 | 后续任务调度阶段再补 |
| SSH 凭据管理还较粗 | 后续可补节点级密钥、连接测试、凭据轮换 |
| GPU 显存等动态指标依赖 InfluxDB 和 worker | 需要部署后持续验证 |

### 阶段判断

节点管理已经可以视为 **管理面 MVP 基本完成**。  
后续主要是生产化、监控部署和调度联动，而不是核心 CRUD 功能缺失。

---

## 4.2 用户管理

**当前进度：约 85%**

用户管理当前已经较完整，尤其是账号安全、导师学生关系、登录会话管理已经具备较高完成度。

| 功能点 | 当前状态 | 说明 |
|---|---|---|
| 用户创建 | 已实现 | 管理员、导师可按权限创建用户 |
| 用户编辑 | 已实现 | 支持基础资料和角色调整 |
| 用户删除 | 已实现 | 删除时联动处理会话 |
| 启用/停用 | 已实现 | 停用后禁止登录 |
| 停用后吊销会话 | 已实现 | 在线 token 会失效 |
| 改密后吊销会话 | 已实现 | 防止旧 token 继续使用 |
| 重置密码后吊销会话 | 已实现 | 管理员或导师重置密码后更安全 |
| 导师-学生关系 | 已实现 | 支持导师管理学生 |
| 学生最多两名导师 | 已实现 | 符合前期需求 |
| 导师管理自己学生 | 已实现 | 权限边界基本建立 |
| 最后管理员保护 | 已实现 | 防止系统无管理员 |
| Linux 子账户映射 | 已实现基础逻辑 | 可创建/规划 Linux 子账户 |
| 用户 home 自动补齐 | 已实现 | 与文件管理衔接更好 |
| 节点所有人候选 | 已复用 | 节点管理可从用户列表选择所有人 |

### 当前剩余问题

| 问题 | 建议 |
|---|---|
| Linux 子账户真实执行依赖 sudoers | 需要在部署文档和脚本中固化 |
| Linux 账户失败回滚还可加强 | 创建用户、设置密码、建目录、设权限应具备事务式补偿 |
| 导师权限需要继续跨模块贯通 | 文件、环境、任务阶段继续按导师学生关系限制访问范围 |
| 用户配额体系尚未完善 | 后续环境和任务阶段需要补磁盘、任务、GPU 使用配额 |

### 阶段判断

用户管理已经进入 **接近完成** 状态。  
当前已经能支撑节点所有人、导师学生文件视图和后续环境/任务权限扩展。

---

## 4.3 文件管理

**当前进度：约 82%**

文件管理已经从“能浏览文件”发展为“可操作、可审计、可持久追踪后台任务的 Web 文件管理器”。同时，它也开始服务于环境管理，作为 zip 环境包、conda 包、whl 包、requirements 文件和源码目录的选择入口。

| 功能点 | 当前状态 | 说明 |
|---|---|---|
| 文件列表 | 已实现 | 支持用户目录浏览 |
| 文本预览 | 已实现 | 可查看文本内容 |
| 图片预览 | 已实现 | 支持浏览器内查看 |
| 音视频预览 | 已实现 | 支持常见媒体预览 |
| 文件下载 | 已实现 | 可下载单文件 |
| multipart 上传 | 已实现 | 可通过 Web 上传文件 |
| 新建目录 | 已实现 | 可维护项目目录结构 |
| 新建文本文件 | 已实现 | 可在 Web 中创建脚本/配置 |
| 保存文本文件 | 已实现 | 支持在线编辑保存 |
| 复制 | 已实现 | 支持文件/目录复制 |
| 移动 | 已实现 | 支持文件/目录移动 |
| 重命名 | 已实现 | 支持文件/目录重命名 |
| 删除 | 已实现 | 支持文件/目录删除 |
| zip 打包 | 已实现 | 支持后台打包 |
| zip/tar 解压 | 后端支持 | 前端主要面向压缩包操作 |
| 打包/解压后台任务 | 已实现 | 避免长任务阻塞 API 请求 |
| 文件任务状态数据库化 | 已实现 | `FileJob` 表已接入 |
| 导师查看学生文件 | 已实现 | 导师可只读查看名下学生文件 |
| 路径边界检查 | 已实现 | 限制用户访问范围 |
| 压缩包路径穿越防护 | 已实现 | 拒绝危险成员 |
| 软链接成员拦截 | 已实现 | 降低解压逃逸风险 |
| 文件操作审计 | 已落库 | 文件操作可追踪 |

### 当前剩余问题

| 问题 | 建议 |
|---|---|
| 上传大小限制仍需确认后端强制执行 | 不能只依赖 Nginx，需要在后端 upload 流程中检查 |
| 普通上传不适合超大文件 | 后续可做分片上传、断点续传 |
| 压缩炸弹检测仍需加强 | 解压前限制总大小、文件数量、嵌套层级 |
| 危险操作二次确认可继续增强 | 删除、覆盖、解压到已有目录应明确提示 |
| 文件任务目前仍是后台线程 | MVP 可以接受，生产环境可考虑独立 worker 或任务队列 |

### 阶段判断

文件管理已经进入 **收尾阶段**。  
当前主要剩余是安全、配额、大文件和生产化能力。

---

## 4.4 环境管理

**当前进度：约 70%–73%**

环境管理已经进入中后段。当前支持环境登记、扫描导入、zip 导入、复制、路径修复、权限修复、检测、本机离线安装和已安装包删除。

| 功能点 | 当前状态 | 说明 |
|---|---|---|
| 环境登记接口 | 已实现 | 登记信息写入 `Env` 表 |
| 环境列表接口 | 已实现 | 读取数据库并同步当前 conda 环境 |
| conda 环境扫描 | 已实现 | 优先 `conda env list --json`，失败后扫描 `conda_env_root` |
| 自动导入现有环境 | 已实现 | 新环境作为 `system_imported` 入库 |
| zip 环境包导入 | 已实现 | 从用户文件区选择 zip 并后台导入 |
| 环境导入状态 | 已实现 | `importing/fixing/testing/available/error` |
| 环境副本 | 已实现 | 可复制已有环境并修复路径 |
| 环境路径修复 | 已实现 | 检测旧 prefix 并替换文本文件中的路径 |
| 环境权限修复 | 已实现 | chown/chmod 与可执行位修复 |
| 环境检测 | 已实现 | 通过 `conda activate` 进入目标环境后执行探针 |
| PyTorch 检测 | 已实现 | 版本、CUDA、cuDNN、GPU 可用性 |
| TensorFlow 检测 | 已实现 | 版本、CUDA、cuDNN、GPU 可用性 |
| 包列表检测 | 已实现 | 优先 `conda list --json` |
| 环境日志文件 | 已实现 | 单环境 JSON Lines 日志文件 |
| 环境操作日志表 | 已实现 | `EnvOperationLog` 记录结构化日志 |
| 环境删除 | 已实现增强 | 删除环境目录和数据库记录，并清理相关包/安装记录 |
| 任务引用检查 | 已增强 | 创建/更新任务时禁止使用非 available/registered 环境 |
| 前端状态轮询 | 已实现 | 环境处于导入/复制/修复/测试状态时自动刷新 |
| 前端日志查看 | 已实现 | 可查看环境操作日志文件 |
| 前端检测展示 | 已实现 | 展示框架和包列表 |
| 本机 conda 离线安装 | 已实现 | 支持 `.tar.bz2`，执行 `conda install --offline -y` |
| 本机 whl 安装 | 已实现 | 支持单 `.whl`，执行 `python -m pip install --no-index` |
| 本机 whl 批量安装 | 已实现 | 支持 `--find-links=<folder> -r requirements.txt` |
| 本机源码目录安装 | 已实现 | 支持 `pip install .` 和 `python setup.py install` |
| 已安装包删除预览 | 已实现 | 后端生成确认文案和卸载命令 |
| 已安装包删除 | 已实现 | 按包来源执行 `conda remove` 或 `pip uninstall` |
| 核心包保护 | 已实现 | Python、pip、setuptools、wheel、conda 等默认保护 |
| 包上传 DB 链路 | 未完成 | `upload_package()` 仍使用 `_PACKAGES` |
| 安装作业 DB 链路 | 未完成 | `install_package()` 仍使用 `_JOBS` |
| 环境安装 worker | 未完成 | worker 扫 DB，但服务层未写 `EnvInstallJob` |
| compile 模式 | 未完成 | 前端显示等待任务调度接入 |
| manifest | 未接入 | `EnvPackageManifest` 表已定义但未使用 |

### 当前需要特别注意的问题

| 问题 | 影响 | 建议 |
|---|---|---|
| 本机安装是同步请求 | 大包安装会长时间占用 API 请求，失败恢复有限 | 改成 EnvInstallJob + worker |
| `_PACKAGES/_JOBS` 仍存在 | 旧安装队列链路仍不持久 | 改为 `EnvPackage` / `EnvInstallJob` |
| `env_install_worker.py` 仍是占位 | 独立安装队列无法工作 | 实现 normal 模式安装 worker |
| 安装成功未写 manifest | 无法精确追踪安装文件和回滚 | 写入 `EnvPackageManifest` 或安装记录 |
| 源码安装可能触发联网依赖解析 | 主节点无网时会失败，或不符合隔离预期 | 加强 `--no-index` 策略或明确提示 |
| 删除包不会自动修复依赖 | 用户可能破坏环境 | 保留确认，建议增加环境快照或回滚提示 |
| 压缩炸弹检测不足 | 导入大压缩包可能占满磁盘 | 限制总大小、文件数量、层级 |
| 导入/复制仍在 API 后台线程 | API 重启会中断，长任务不稳定 | 迁移为独立 worker |
| `list_envs()` 过滤条件需确认 | 手动 registered 环境若未被扫描到，可能不显示 | 建议显示所有可见 `registered/available` 环境 |
| 环境操作日志 DB 已有，但前端主要读文件日志 | 结构化日志检索能力尚未释放 | 后续可加环境操作日志分页查询 |

### 阶段判断

环境管理已经进入 **中后段开发**。  
下一步不是继续增加同步安装类型，而是应当将当前可用的同步安装能力迁移成更稳定的数据库作业和 worker 模式。

---

## 4.5 任务管理

**当前进度：约 25%–30%**

任务管理当前仍未进入重点开发阶段。按照当前开发顺序，这是合理现象，不应作为现阶段主要扣分项。

| 功能点 | 当前状态 | 说明 |
|---|---|---|
| 任务提交接口 | 已有 | 但写入内存 `_TASKS` |
| 任务列表/详情 | 已有 | 读取内存状态 |
| 任务取消 | 已有 | 主要操作内存任务 |
| 任务重提 | 已有 | 仍基于内存 |
| 任务日志 | 占位 | 未读取真实日志 |
| 任务数据持久化 | 未完成 | `Task` 表已定义但未接入 |
| 任务资源需求 | 表已定义 | 服务层未完整接入 |
| 调度器 | 占位扫描 | 只扫描 DB 等待任务 |
| 执行器 | 占位扫描 | 只扫描 dispatching 任务 |
| Runtime Guard | 占位扫描 | 未检查 PID/GPU |
| GPU 分配记录 | 表已定义 | 需要调度器写入 |
| 任务事件流 | 表已定义 | 需要后续接入 |

### 当前不作为主要扣分项的原因

任务管理依赖前面的模块：

1. 节点管理要先提供节点和 GPU 资源。
2. 用户管理要先提供用户、角色、权限、导师学生关系。
3. 文件管理要先提供脚本、配置、结果文件的 Web 管理能力。
4. 环境管理要先提供可运行环境。
5. 任务管理最后才能稳定进行提交、调度、执行和监管。

因此，当前任务管理未完成符合开发节奏。

---

## 5. 数据固化情况

### 5.1 已经持久化或真实落地

| 对象 | 当前存储 | 状态 |
|---|---|---|
| 用户账号 | PostgreSQL `users` | 已持久化 |
| 用户状态 | PostgreSQL `users.state` | 已持久化 |
| 用户 home / Linux 账户映射 | PostgreSQL `users.home_path`, `users.linux_account_name` | 已持久化 |
| 导师-学生关系 | PostgreSQL `user_supervisors` | 已持久化 |
| 登录会话/IP/设备 | PostgreSQL `login_sessions` | 已持久化 |
| 会话吊销状态 | PostgreSQL `login_sessions.revoked_at/logout_at` | 已持久化 |
| 节点 | PostgreSQL `nodes` | 已持久化 |
| 节点多所有人 | PostgreSQL `nodes.owner_user_ids` | 本轮确认已接入 |
| 节点访问范围 | PostgreSQL `nodes.access_scope` | 本轮确认已接入 |
| 节点共享范围 | PostgreSQL `nodes.sharing_scope` | 本轮确认已接入 |
| GPU 清单 | PostgreSQL `gpus` | 已持久化 |
| 当前 GPU 分配 | PostgreSQL `task_allocations` | 表已定义，等待任务阶段接入 |
| 审计日志 | PostgreSQL `audit_logs` | 已持久化 |
| 系统设置 | PostgreSQL `settings` | 已持久化 |
| 文件任务 | PostgreSQL `file_jobs` | 已持久化 |
| 环境本体 | PostgreSQL `envs` | 已接入服务层 |
| 环境操作日志 | PostgreSQL `env_operation_logs` + 文件日志 | 已接入 |
| 节点/GPU 指标 | InfluxDB | 基础逻辑已存在 |
| 文件内容 | 文件系统 | 已真实落盘 |
| 文件上传/下载 | 文件系统 | 已实现 |
| 文件新建/保存/复制/移动/重命名/删除 | 文件系统 | 已实现 |
| 文件打包/解压结果 | 文件系统 | 已实现 |
| 环境导入/复制结果 | 文件系统 conda envs 目录 | 已真实落盘 |
| 环境检测结果 | 实时探针返回 + 日志记录 | 已实现 |
| 本机环境包安装输出 | 环境日志 + EnvOperationLog | 已实现 |
| 已安装包删除输出 | 环境日志 + EnvOperationLog | 已实现 |

### 5.2 仍在内存或占位

| 对象 | 当前实现 | 风险 |
|---|---|---|
| EnvPackage 旧上传链路 | `_PACKAGES` | 包上传元数据重启丢失，worker 无法消费 |
| EnvInstallJob 旧安装作业链路 | `_JOBS` | 服务层写内存，worker 扫 DB，链路断开 |
| EnvPackageManifest | 表已定义但未使用 | 后续安装包追踪和可选卸载需要接入 |
| compile 模式 | 请求模型已有但未执行 | 需要指定节点/GPU 编译能力 |
| 任务 | `_TASKS` | 任务阶段尚未开始，后续需要迁移到数据库 |
| 任务日志 | 占位文本 | 后续任务阶段需要读取真实日志 |
| Runtime Guard 详情 | 占位对象 | 后续任务阶段需要实现 PID/GPU 检查 |

---

## 6. 当前阶段性评价

按照实际开发顺序，本轮更正后的评价如下：

| 阶段 | 当前完成度 | 阶段判断 |
|---|---:|---|
| 节点管理 | 82%–85% | 管理面 MVP 基本完成，剩余监控部署、错误码优化和资源池化 |
| 用户管理 | 约 85% | 接近完成，剩余跨模块权限贯通和 Linux 账户细节 |
| 文件管理 | 约 82% | 进入收尾阶段，剩余大文件、安全和任务队列增强 |
| 环境管理 | 70%–73% | 已进入中后段，本体、导入、检测、本机包安装、包删除均有实现 |
| 任务管理 | 25%–30% | 依赖前面模块，当前未完成符合开发节奏 |

总体判断：

> 当前 NebulaGrid 3.0 的前置模块已经比较扎实：节点管理、用户管理、文件管理基本能支撑后续环境和任务模块。环境管理也已经具备实际使用价值。下一步应优先把环境安装链路 worker 化，再进入任务管理主链路。

---

## 7. 当前最值得肯定的改进

### 7.1 节点管理管理面基本完成

本轮最重要的更正点是节点管理。当前已经支持：

- 新增节点；
- 修改节点；
- 删除节点；
- 重连节点；
- 强制下线节点；
- 多所有人；
- public/private 访问范围；
- none/group/public 共享范围；
- GPU 型号顺序保存；
- 所有人搜索选择；
- 普通用户按权限查看节点；
- 删除节点时清理调度引用；
- 节点操作审计。

这已经明显超过“基础登记”，可以视为节点管理 MVP 基本完成。

### 7.2 节点可见性开始服务后续调度

节点可见性不再只是管理员后台字段，而是已经影响 `/api/nodes` 返回结果。普通用户只能看到自己有权使用的节点，这为后续任务提交时选择节点、限制 GPU 资源打基础。

### 7.3 前端节点管理体验明显提升

前端已经支持：

- 节点表格展示所有人、访问范围、共享范围；
- 修改节点；
- 删除节点；
- 重连；
- 强制下线；
- 所有人搜索；
- 多选 owner；
- 左侧导航固定。

这说明节点管理不只是后端完成，前端管理体验也同步推进。

### 7.4 已有实际接口测试记录

`test_result.md` 记录了完整的管理员节点管理测试，包括新增、修改、可见性、重连、下线、删除、前端静态文件检查。  
虽然这还不是自动化测试，但已经是很好的阶段验收材料。

---

## 8. 当前主要风险

| 风险 | 所属阶段 | 影响 | 建议 |
|---|---|---|---|
| GPU 数量/型号不一致时返回 500 | 节点管理 | 前端难以友好提示，错误语义不准确 | 改为 400/422，并返回明确文案 |
| 节点监控部署仍未完全工程化 | 节点管理 | 真实集群中节点状态和 GPU 指标可能不稳定 | 补 systemd、远端脚本同步、SSH 失败重试 |
| 节点资源池/队列/标签未完成 | 节点/任务管理 | 后续复杂调度缺少资源分组依据 | 任务阶段前补节点标签和队列 |
| 本机环境安装是同步请求 | 环境管理 | 大包安装会长时间占用 API 请求 | 改成 EnvInstallJob + worker |
| `_PACKAGES/_JOBS` 仍存在 | 环境管理 | 旧安装队列链路仍不持久 | 改为 `EnvPackage` / `EnvInstallJob` |
| `env_install_worker.py` 仍是占位 | 环境管理 | 独立安装队列无法工作 | 实现 normal 模式安装 worker |
| 安装成功未写 manifest | 环境管理 | 无法精确追踪安装文件和回滚 | 写入 `EnvPackageManifest` 或安装记录 |
| 压缩炸弹检测不足 | 文件/环境管理 | 导入大压缩包可能占满磁盘 | 限制总大小、文件数量、层级 |
| 任务服务仍是内存 | 任务管理 | 未来调度器看不到任务 | 任务阶段首要迁移 |
| Runtime Guard 尚未实现 | 任务管理 | 用户仍可绕过 GPU 分配 | 任务阶段实现 PID/GPU 检查 |

---

## 9. 下一阶段建议路线

按照当前开发顺序，建议后续分两条线推进：一条是节点管理收尾，一条是环境管理收尾。

### 阶段 A：节点管理收尾

| 优先级 | 任务 |
|---:|---|
| 1 | 修复 `gpu_count` 与 `gpu_models` 不一致时返回 HTTP 500 的问题 |
| 2 | 将 `test_result.md` 中的节点管理用例沉淀为自动化测试 |
| 3 | 补节点编辑/删除/可见性的 pytest 集成测试 |
| 4 | 补节点 owner group 共享可见性测试 |
| 5 | 补节点监控 worker systemd 文件 |
| 6 | 补远端 monitor 脚本同步工具 |
| 7 | 后续任务阶段前增加节点标签/队列/资源池字段 |

### 阶段 B：环境安装链路数据库化

| 优先级 | 任务 |
|---:|---|
| 1 | 移除或冻结 `_PACKAGES/_JOBS` 旧链路 |
| 2 | `upload_package()` 改为写入 `EnvPackage` |
| 3 | 支持真实 multipart 包上传到 `env_package_root` |
| 4 | 计算 sha256、大小、包类型 |
| 5 | `install_package()` 改为写入 `EnvInstallJob` |
| 6 | `get_install_job()` / `cancel_install_job()` 从数据库读取和更新 |
| 7 | 前端展示数据库安装作业状态 |

### 阶段 C：环境安装 worker MVP

| 优先级 | 任务 |
|---:|---|
| 1 | `env_install_worker.py` 消费 queued job |
| 2 | normal 模式执行 conda/pip 安装 |
| 3 | 写 started_at / finished_at / return_code |
| 4 | 安装 stdout/stderr 写入日志 |
| 5 | 安装失败回写 failed |
| 6 | 安装成功回写 succeeded |
| 7 | 安装完成后自动触发环境检测 |
| 8 | 前端轮询安装 job 状态 |

### 阶段 D：环境安装安全和稳定性

| 优先级 | 任务 |
|---:|---|
| 1 | 给源码安装增加离线约束或明显提示 |
| 2 | 增加环境安装锁，防止同一环境并发安装/删除 |
| 3 | 安装失败后的状态恢复 |
| 4 | 删除包前提示依赖风险 |
| 5 | 增加环境空间统计 |
| 6 | 增加包安装历史页面 |
| 7 | 增加 EnvOperationLog 查询接口和前端页面 |

### 阶段 E：任务管理准备

环境管理基本稳定后再集中推进任务管理：

| 优先级 | 任务 |
|---:|---|
| 1 | 任务数据库化 |
| 2 | 调度器 MVP |
| 3 | 执行器 MVP |
| 4 | 日志读取 |
| 5 | 任务取消和回收 |
| 6 | Runtime Guard |
| 7 | `alloc_error` |
| 8 | 历史任务和统计 |

---

## 10. 测试与工程化建议

### 10.1 本次静态检查结果

本次对 `backend/app` 下 Python 文件进行了 AST 语法解析检查：

| 检查项 | 结果 |
|---|---|
| Python 文件数量 | 65 |
| 语法错误 | 未发现 |

### 10.2 仓库内已有实际测试记录

仓库中存在 `test_result.md`，其中记录了 2026-05-22 对管理员节点管理功能的接口测试。测试结果显示：

| 测试项 | 结果 |
|---|---|
| 健康检查 | 通过 |
| 管理员登录 | 通过 |
| 所有人候选用户接口 | 通过 |
| 节点列表初始查询 | 通过 |
| 新增节点 | 通过 |
| 新增后列表校验 | 通过 |
| GPU 数量/型号校验 | 功能拒绝非法请求，但错误码为 500 |
| 修改节点 | 通过 |
| 私有不共享：所有人可见性 | 通过 |
| 私有不共享：非所有人可见性 | 通过 |
| 重连节点 | 通过 |
| 强制下线节点 | 通过 |
| 删除节点 | 通过 |
| 临时数据清理 | 通过 |
| 前端静态文件检查 | 通过 |

这说明节点管理的核心接口链路已经经过一次实际环境验收。

### 10.3 pytest 运行说明

当前分析沙箱仍不具备完整项目依赖，无法可靠执行完整集成测试。建议在正式开发环境中执行：

```bash
cd backend
python -m pip install -e ".[dev]"
export NEBULAGRID_DATABASE_URL=sqlite:///./test_nebulagrid.db
python -m pytest -q
```

或使用独立 PostgreSQL 测试库进行更接近生产环境的测试。

### 10.4 建议新增自动化测试

| 模块 | 建议测试 |
|---|---|
| 节点管理 | 新增、修改、删除、重连、强制下线 |
| 节点字段校验 | `gpu_count` 与 `gpu_models` 不一致应返回 400/422 |
| 节点所有人 | 多 owner 保存顺序和去重 |
| 节点可见性 | public/private、none/group/public 共享 |
| 节点 group 共享 | 学生 owner、导师 owner 的共享展开 |
| 节点删除 | 清理 TaskRequirement、EnvInstallJob、TaskAllocation、TaskRuntimeGuard |
| 文件管理 | 上传大小限制、非法路径、压缩包路径穿越、软链接成员、删除根目录保护 |
| 导师文件视图 | 只能查看名下学生，且只读 |
| 环境扫描 | conda 命令失败时回退扫描目录 |
| 环境导入 | zip 路径合法、路径穿越、软链接成员、缺少 python 的失败路径 |
| 环境安装 | conda 包、whl、requirements、源码目录 |
| 包删除 | 不存在包、核心保护包、conda/pip 分源命令 |
| EnvOperationLog | 安装、删除、检测是否写入数据库 |
| 用户管理 | 停用、删号、重置密码后的 token 是否失效 |

### 10.5 工程化建议

1. 尽快引入 Alembic，替代手写补列迁移。
2. 补齐 systemd unit 文件。
3. 补齐 Nginx 模板，明确 `client_max_body_size`。
4. 补远端脚本同步工具，至少同步 `monitor.py`、`runner.py`、`env_installer.py`、`env_probe.py`。
5. 将环境导入、环境安装、文件重 IO 操作逐步独立 worker 化。
6. 为环境包安装和编译预留独立队列，不与普通训练任务调度混用。
7. 给环境安装命令增加更明确的超时、取消和失败恢复策略。
8. 在进入任务管理前，建议先把节点管理自动化测试和环境安装 worker 补齐。

---

## 11. 最新总体判断

当前 NebulaGrid 3.0 的阶段完成度约为 **73%–75%**。

分阶段看：

| 阶段 | 完成度 |
|---|---:|
| 节点管理 | 82%–85% |
| 用户管理 | 约 85% |
| 文件管理 | 约 82% |
| 环境管理 | 70%–73% |
| 任务管理 | 25%–30% |

本次更正后的核心判断是：

1. **22 日这版的主要进展是节点管理完成度显著提升。**
2. 节点管理已经具备新增、修改、删除、重连、强制下线、多所有人、共享范围、权限可见性和前端搜索选择。
3. 仓库内 `test_result.md` 记录了节点管理实际接口测试结果，核心链路通过。
4. 环境管理保持 21 日版本的中后段状态，具备环境维护能力，但异步安装队列仍未完成。
5. 任务管理仍符合当前开发顺序，尚未进入主开发阶段。

按照当前开发路线，下一阶段建议优先完成：

> 节点错误码与自动化测试收尾 → 环境安装链路数据库化 → env_install_worker 真正执行安装 → 环境锁与 manifest → 再进入任务管理。
