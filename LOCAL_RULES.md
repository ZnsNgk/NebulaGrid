# NebulaGrid 本地规则文件

<!--
为什么新增这个文件：
- 之前的 `.cursor/rules/local-safety-rules.mdc` 位于隐藏目录 `.cursor/` 中，很多文件浏览器默认不会显示隐藏目录。
- 这个 `LOCAL_RULES.md` 放在仓库根目录，名称直观、默认可见，方便你直接在仓库首页或文件列表中找到。
- 本次操作只新增这个文件，不删除、不移动、不覆盖已有文件。
-->

## 1. 修改任何文件前，先检查当前仓库状态

<!--
目的：
修改前先看清楚当前分支和工作区状态，避免把已有改动误认为自己刚产生的改动。
这一步也能帮助发现是否存在未提交文件、删除标记或其他异常状态。
-->

- 执行 `git status --short --branch`，确认当前分支和文件状态。
- 如果发现已有未提交变更，先确认这些变更是否和当前任务有关。
- 不要覆盖、重置或格式化与当前任务无关的文件。

## 2. 不要误删文件

<!--
目的：
误删通常发生在删除命令范围过大、路径写错、或没有复核 `git status` 的情况下。
因此，任何删除、移动、重命名操作都必须先确认影响范围。
-->

- 禁止执行没有明确目标路径的批量删除命令，例如 `rm -rf *` 或 `find . -delete`。
- 不删除隐藏目录、依赖目录、缓存目录或生成目录，除非需求明确要求这样做。
- 如果 `git status --short` 出现非预期的 `D` 删除标记，应立即停止并排查原因。

## 3. 保持变更尽可能小

<!--
目的：
每次只改当前任务需要的内容，可以降低审查成本，也能减少误改和合并冲突。
-->

- 只新增或修改当前需求直接相关的文件。
- 不做无关重构、无关格式化、无关依赖升级。
- 不顺手调整与任务无关的配置、文档或脚本。

## 4. 新增代码、脚本或配置时要写详细注释

<!--
目的：
注释应解释设计原因、风险点和边界条件，而不仅仅重复代码本身。
这样后续维护者可以理解为什么要这样写，以及修改时要注意什么。
-->

- 新增配置时，说明关键字段的用途和影响范围。
- 新增脚本时，说明主要步骤、输入输出、失败条件和安全限制。
- 新增代码时，说明复杂逻辑、非显而易见的判断和可能的副作用。

## 5. 提交前复核，确认没有误删

<!--
目的：
提交前做最后一次核对，确保最终提交只包含预期内容。
如果检查结果显示异常，应先修正再提交。
-->

- 执行 `git status --short --branch`，确认没有非预期删除或无关改动。
- 执行 `git diff --stat`，确认改动文件数量和规模合理。
- 对关键新增或修改文件执行内容检查，例如 `nl -ba LOCAL_RULES.md | sed -n '1,220p'`。
- 确认无误后再提交。
