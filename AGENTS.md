# NebulaGrid 仓库协作规则

<!--
本文件位于仓库根目录，因此规则适用于整个 NebulaGrid 仓库。
创建本文件的目的，是把本地安全规则放进仓库中，方便所有协作者和自动化代理在修改代码前阅读并遵守。
-->

## 1. 修改前先确认仓库状态

<!--
在开始任何文件修改之前，先确认当前分支和工作区状态。
这样可以避免覆盖他人的改动，也能提前发现仓库里是否已经存在未提交变更。
-->

- 运行 `git status --short --branch` 查看当前分支和工作区状态。
- 如果发现已有未提交变更，先判断这些变更是否属于当前任务。
- 不要随意覆盖、格式化或重置与当前任务无关的文件。

## 2. 严格避免误删文件

<!--
误删文件通常来自范围过大的删除命令或没有确认目标路径的清理操作。
因此，任何删除、移动、重命名文件的操作都必须先确认目标路径和影响范围。
-->

- 不要运行没有明确目标的批量删除命令，例如 `rm -rf *`、`find . -delete`。
- 不要删除依赖目录、缓存目录、生成物或隐藏目录，除非任务明确要求。
- 如果 `git status --short` 中出现非预期的 `D` 删除标记，应立即停止并排查原因。

## 3. 保持变更范围最小

<!--
每次提交只应该包含完成当前需求所必需的内容。
保持小范围变更可以降低审查难度，也能减少误改、误删和合并冲突的风险。
-->

- 只创建或编辑当前任务直接需要的文件。
- 不做无关重构、无关格式化或无关依赖升级。
- 不修改与当前需求无关的配置文件、脚本或文档。

## 4. 代码和配置需要详细注释

<!--
新增代码、脚本或规则时，注释应解释“为什么这样做”和“需要注意什么”。
注释不要只重复代码表面含义，而应说明设计目的、风险点和边界条件。
-->

- 新增规则文件时，为关键字段和每个规则段落添加说明。
- 新增脚本时，注释主要步骤、输入输出、失败条件和安全限制。
- 新增代码时，注释复杂逻辑、非显而易见的判断和潜在副作用。

## 5. 提交前必须复核

<!--
提交前复核用于确认最终变更与用户需求一致，并确保没有意外删除文件。
如果检查结果不符合预期，应先修正问题，再进行提交。
-->

- 运行 `git status --short --branch`，确认变更清单符合预期。
- 运行 `git diff --stat`，确认文件数量和改动规模合理。
- 对新增或修改的关键文件运行内容检查，例如 `sed -n`、`nl -ba` 或相关测试命令。
- 确认没有非预期删除、重命名或大范围无关改动后，再提交。
